{% extends "base.html" %}


{% block bodycont %}

                
        {% if registered %}
        <strong>Thank you for registering!</strong>
        <a href="/textform">Return to the homepage.</a><br />
        {% else %}


        <form id="user_form" method="post" action="/auth/register/">

            {% csrf_token %}
		        {{ form.non_field_errors }}
                
                {{ form.username.errors }}
		        <label for="{{ form.username.id_for_label }}">Your username:</label>
                {{ form.username }}
<!--
                <fieldset>
                    <legend>Registration</legend>
                    <div>
                        <label for="inputLogin">Login</label>
                        <div>
                            <input class="form-control" placeholder="Login" type="text" name="username">
                        </div>
                    </div>
                    <div>
                        <label for="inputPassword">Password</label>
                        <div>
                        <input placeholder="Password" type="password" name="password">
                      
                        </div>
                    </div>
                   
                    <div>
                        <label for="inputNickName">E-mail</label>
                        <div>
                            <input placeholder="E-mail" type="text" name="email">                        
                        </div>
                    </div>

                    <div>
                        <label for="inputNickName">Website</label>
                        <div>
                            <input placeholder="Website" type="text" name="website">                           
                        </div>
                    </div>
                    
                </fieldset> 
-->
            <input type="submit" name="submit" value="Register"/>
        </form>
        {% endif %}


					
{% endblock %}
